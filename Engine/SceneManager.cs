namespace WhgVedit.Engine;

// This class is currently unused but not deleted for now.
// Use Scene.Main to access the current scene instead.

static class SceneManager
{
	public static Scene? CurrentScene { get; set; }
}